# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Access ---
# The chair: the single account with full roster + console control.
MODERATOR_EMAIL=
# Owner id for the governing-document corpus (wiki_pages). Must match your seed.
CORPUS_USER_ID=

# --- AI ---
# Parliamentarian + threshold classifier
ANTHROPIC_API_KEY=
# Embeddings for semantic corpus search (text-embedding-3-small)
OPENAI_API_KEY=

# --- Email (Resend): sign-in codes + team invites ---
RESEND_API_KEY=
# Verified sender; falls back to Resend's onboarding domain for testing.
MAIL_FROM=GA Moderator <onboarding@resend.dev>

# --- App ---
APP_URL=http://localhost:3000
